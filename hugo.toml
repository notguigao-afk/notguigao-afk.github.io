baseURL = 'https://notguigao-afk.github.io/'
languageCode = 'zh-cn'
title = '我的个人博客'
theme = 'PaperMod'

[params]
  # Landing hero — parallel English quote lines (not title/subtitle)
  [params.homeHero]
    quoteLines = [
      "Live as if you were to die tomorrow.",
      "Learn as if you were to live forever."
    ]

  ShowCodeCopyButtons = true
